node_modules
.next
*.tsbuildinfo
next-env.d.ts
.DS_Store

# env files — secrets never get committed; only the example template is tracked
.env*
!.env.example

# reference projects — design/code references only, not part of this app
dashboard-ref/
admin/
.vercel
